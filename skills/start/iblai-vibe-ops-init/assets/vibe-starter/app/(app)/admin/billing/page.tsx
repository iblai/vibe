"use client";

import { AccountPanel } from "@/components/admin/account-panel";

/** Plan & Credits · Spend Limits · Agent Limits (see /iblai-vibe-billing). */
export default function AdminBillingPage() {
  return <AccountPanel tab="billing" />;
}
